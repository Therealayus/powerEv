# Local dev: point to your backend (default if unset: /api, i.e. same-origin proxy)
VITE_API_URL=http://localhost:5000/api

# Production (Vercel): set in project Settings → Environment Variables, e.g.
# VITE_API_URL=https://ev-charging-api-euv0.onrender.com/api
